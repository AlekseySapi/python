# Кортеж - это упорядоченная неизменяемая коллекция элементов.
# После создания кортежа его элементы нельзя изменить.

# Пустой кортеж
my_tuple = ()

# Кортеж с элементами
coordinates = (10.0, 20.0, 30.0, 40.0, 50.0)

print(coordinates[0])   # Вывод: 10.0
print(coordinates[1:4]) # Вывод: (20.0, 30.0)